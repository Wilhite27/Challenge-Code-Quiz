var timerEl = document.getElementsByClassName('time-tracker')
function wrongFunction() {
    window.alert('Wrong!!!');
}
var mainEl = document.getElementById('')
var timeLeft = 90; 
var timeInterval = setInterval(function() {
    timeLeft--
    console.log(timeLeft)
    },1000);
        
        var answer-choices = ["wrong-choice", "right-choice"]
            for (x of answers);{
                if (x != ["right-choice"]) {
                    
                
       timeInterval(